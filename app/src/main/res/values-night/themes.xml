<resources>
    <!-- Base application theme. -->
    <style name="Theme.WalltakerExplorer" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:titleTextStyle">@color/walltaker</item>

        <!-- Customize your theme here. -->
    </style>
</resources>